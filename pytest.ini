[pytest]
testpaths = tests
pythonpath = .
markers =
    e2e: Playwright browser-level end-to-end test against a live server subprocess. Excluded from the default run (needs `playwright install chromium`); run explicitly with `pytest -m e2e`.
addopts = -m "not e2e"
